To ssh into ec2 instance use following command, 
    ssh -i file.pem username@ip-address

        Here -i specifies that alternate identification file for public key authentication and file.pem is the name of .pem file